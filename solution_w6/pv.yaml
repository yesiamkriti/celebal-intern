apiVersion: v1
kind: PersistentVolume
metadata:
  name: my-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:                     # For Minikube/local use only
    path: "/mnt/data"
  persistentVolumeReclaimPolicy: Retain
